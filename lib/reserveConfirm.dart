import 'package:flutter/material.dart';

class ReserveConfirm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          SizedBox(
            height: 1000,
          ),
          Text(
            '예약 완료',
          ),
        ],
      ),
    );
  }
}
